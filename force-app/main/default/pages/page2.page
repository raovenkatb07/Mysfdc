<apex:page controller="page2" showHeader="false" >
    <apex:form >
    <apex:pageBlock >
        <marquee>Account inf</marquee>
        <apex:pageBlockSection >
            <apex:inputField value="{!a.name}" />
            <apex:inputField value="{!a.rating}" />
            <apex:inputField value="{!a.industry}" />
            <apex:inputField value="{!a.type}" />
        </apex:pageBlockSection>
        <apex:pageBlockButtons >
        <apex:commandButton value="Save" action="{!ins}" />
            <apex:commandLink value="Click" action="{!ins}" />
        </apex:pageBlockButtons>
        </apex:pageBlock>
    </apex:form>
</apex:page>